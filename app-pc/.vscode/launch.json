{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debug",
            "cwd": "${workspaceFolder}",
            "request": "launch",
            "type": "debugpy",
            "program": "${file}",
            "console": "integratedTerminal",
        }
    ]
}